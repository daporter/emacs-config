# name: file { ... }
# key: file
# --
file { '${1:<name>}':
  ensure => ${2:<ensure>},
  owner => '${3:<owner>}',
  group => '${4:<group>}',
  mode => '${5:<mode>}',
  content => '${6:<content>}',
  source => '${7:<source>}',
  template => '${8:<template>}',
  require => ${9:<require>},
  notify => ${10:<notify>},
}