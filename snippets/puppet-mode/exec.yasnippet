# name: exec { ... }
# key: exec
# --
exec { '${1:<name>}':
  command => '${2:<command>}',
  cwd => '${3:<cwd>}',
  creates => '${4:<creates>}',
  path => '${5:<path>}',
  require => ${6:<require>},
  notify => ${7:<notify>},
}